// localStorage key set when onboarding finishes; the reader surface reads it
// to decide whether to show a one-shot coach overlay on the first entry view.
export const ONBOARDING_COACH_FLAG_KEY = "flash:onboarding-coach-pending"
