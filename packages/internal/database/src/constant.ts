// PostgreSQL connection configuration
export const { POSTGRES_URL } = process.env // add URL here to develop for now

export const { SUPABASE_URL } = process.env
export const { SUPABASE_ANON_KEY } = process.env
